version: "3.8"
services:
  server:
    build: ./server
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - MONGO_URI=mongodb+srv://spicy7577_db_user:EX1bfRWurn1pIs6K@zamazon.ywgygek.mongodb.net/zamazon?retryWrites=true&w=majority&appName=zamazon
      - SMTP_HOST=smtp.ethereal.email
      - SMTP_USER=joshua.mayer@ethereal.email
      - SMTP_PASS=mWHP5PjCWj1Q94rW1s
    volumes:
      - ./server:/app
      - /app/node_modules

  client:
    build: ./client
    ports:
      - "80:5173"
    environment:
      - VITE_API_URL=http://localhost:5000
    volumes:
      - ./client:/app
      - /app/node_modules
    depends_on:
      - server
  # mongo:
  #   image: mongo
  #   ports:
  #     - "27017:27017"
  #   volumes:
  #     - mongo-data:/data/db

  # mongo-express:
  #   image: mongo-express
  #   restart: always
  #   ports:
  #     - "8081:8081"
  #   environment:
  #     ME_CONFIG_MONGODB_SERVER: mongo
  #     ME_CONFIG_MONGODB_PORT: 27017
  #   depends_on:
  #     - mongo

volumes:
  mongo-data:
